package gameLogic;

public class NPC extends Mob {
    public NPC(int x, int y, String name, String description,  char icon, int maxHealth, int armor, int level, int strength, int dexterity, int intelligence) {
        super(x, y, name, description,  icon, maxHealth, armor, level, strength, dexterity, intelligence);
    }
}
